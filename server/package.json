{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "socket.io": "^4.7.4"
  },
  "devDependencies": {
    "typescript": "^4.4.2"
  },
  "scripts": {
    "build": "rm -rf build && tsc",
    "start": "node build/index.js"
  }
}
